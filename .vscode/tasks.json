{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "test submit",
      "type": "shell",
      "command": "make",
      "args": [
        "all"
      ],
      "options": {
        "cwd": "${fileDirname}"
      }
    },
    {
      "label": "precision submit",
      "type": "shell",
      "command": "make",
      "args": [
        "precision"
      ],
      "options": {
        "cwd": "${fileDirname}"
      }
    },
    {
      "label": "just build",
      "type": "shell",
      "command": "make",
      "args": [
        "a.out"
      ],
      "options": {
        "cwd": "${fileDirname}"
      }
    }
  ]
}